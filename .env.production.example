# Production Environment Configuration for Campus Nutrition App
# Copy this to .env and update with your actual values

# =============================================================================
# CORE APPLICATION SETTINGS
# =============================================================================
ENVIRONMENT=production
SECRET_KEY=your-super-secret-jwt-key-here-change-this
DEBUG=false

# =============================================================================
# HTTPS/SSL CONFIGURATION
# =============================================================================
# Frontend and backend URLs (HTTPS for production)
FRONTEND_URL=https://your-domain.com
BACKEND_URL=https://your-domain.com

# SSL certificates (optional - if not using nginx/proxy)
# SSL_KEYFILE=/path/to/ssl/private.key
# SSL_CERTFILE=/path/to/ssl/certificate.crt

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/nutritionapp?retryWrites=true&w=majority

# =============================================================================
# GOOGLE OAUTH CONFIGURATION
# =============================================================================
# Get these from Google Cloud Console (https://console.cloud.google.com)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# AI AGENT CONFIGURATION
# =============================================================================
# OpenAI API key for nutrition AI assistant
OPENAI_API_KEY=sk-your-openai-api-key

# Tavily API key for web search functionality
TAVILY_API_KEY=tvly-your-tavily-api-key

# =============================================================================
# EMAIL CONFIGURATION (for password reset)
# =============================================================================
# Gmail SMTP settings (or your email provider)
SMTP_USER=your-app-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password
FROM_EMAIL=your-app-email@gmail.com
FROM_NAME=Campus Nutrition

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Server binding
HOST=0.0.0.0
PORT=8000

# Worker processes (auto-calculated if not set)
# WORKERS=4

# =============================================================================
# SECURITY HEADERS (already configured in code)
# =============================================================================
# These are handled automatically by the security middleware:
# - Strict-Transport-Security (HSTS)
# - Content-Security-Policy (CSP)
# - X-Frame-Options
# - X-Content-Type-Options
# - X-XSS-Protection
# - Referrer-Policy

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# 1. Never commit this file to version control
# 2. Use strong, unique passwords and secrets
# 3. Update Google OAuth URLs when deploying
# 4. Test SSL certificate before going live
# 5. Monitor application logs for errors